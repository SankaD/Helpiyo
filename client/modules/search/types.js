import helper from '../utils/type_helper';

const NAME = 'SEARCH';

export const SEARCH = helper(NAME, 'SEARCH');
export const SEACH_RESULTS_RECEIVED = helper(NAME, 'SEACH_RESULTS_RECEIVED');
export const SEARCH_FAILED = helper(NAME, 'SEARCH_FAILED');

export const FILTER_CHANGED = helper(NAME, 'FILTER_CHANGED');
