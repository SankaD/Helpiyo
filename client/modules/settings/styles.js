import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  textInput: {
    backgroundColor: '#FFF',
    marginBottom: 2,
    // borderWidth: 0.5,
  },
});

export default styles;
