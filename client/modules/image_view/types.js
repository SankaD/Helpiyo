import helper from '../utils/type_helper';

const NAME = 'ImageView';

export const SHOW_IMAGE = helper(NAME, 'SHOW_IMAGE');
export const SHOW_IMAGE_FAILED = helper(NAME, 'SHOW_IMAGE_FAILED');
export const IMAGE_SHOWED = helper(NAME, 'IMAGE_SHOWED');
