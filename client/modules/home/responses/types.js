import helper from '../../utils/type_helper';

const NAME = 'MY_RESPONSES';

export const LOAD_RESPONSES = helper(NAME, 'LOAD_RESPONSES');
export const RESPONSES_LOADED = helper(NAME, 'RESPONSES_LOADED');
export const LOAD_RESPONSES_FAILED = helper(NAME, 'LOAD_RESPONSES_FAILED');


export const SHOW_RATING_MODAL = helper(NAME, 'SHOW_RATING_MODAL');
export const HIDE_RATING_MODAL = helper(NAME, 'HIDE_RATING_MODAL');

