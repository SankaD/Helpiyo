import helper from '../../utils/type_helper';

const NAME = 'MY_REQUESTS';

export const LOAD_REQUESTS = helper(NAME, 'LOAD_REQUESTS');
export const REQUESTS_LOADED = helper(NAME, 'REQUESTS_LOADED');
export const LOAD_REQUEST_FAILED = helper(NAME, 'LOAD_REQUEST_FAILED');


export const SHOW_RATING_MODAL = helper(NAME, 'SHOW_RATING_MODAL');
export const HIDE_RATING_MODAL = helper(NAME, 'HIDE_RATING_MODAL');

